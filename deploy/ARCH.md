# deploy
Deployment-only artifacts that live beside the main dashboard app.
Folders here are copied to target machines instead of bundled into the Node build.
Current contents focus on the `md` / `agent_2` dashboard adapter package.
一旦我所属的文件夹有所变化，请更新我。

| file name | position | function |
| --- | --- | --- |
| agent_2_dashboard_adapter/ | deployable package | Python adapter that should be deployed to `md` / `agent_2` |
